# Roadmap

## 当前阶段

约 168 秒 Remotion 预览视频、分镜与口播旁白已实现并可在 Studio 播放，内容已按独立目录组织，待浏览器可视化验收与兼容环境下的完整 MP4 导出。

## 已完成

- 建立项目规范与进度文档。
- 使用官方空白模板初始化 Remotion 4.0.523 TypeScript 项目。
- 安装 npm 依赖并生成 `package-lock.json`。
- 通过 ESLint、TypeScript 类型检查和 Remotion bundle。
- 启动 Remotion Studio，确认 `http://localhost:3000` 页面和 `bundle.js` 均返回 HTTP 200。
- 从 `chushang.party` 的实际样式表提取配色、字体、字号、行高、字距与字重，建立 `src/theme.ts`。
- 将 `article.md` 拆分为 7 个场景、总计 60 秒的 `storyboard.json`。
- 按屏显字符数和每秒 5 字规则生成 7 场动态时长，总计 60 秒。
- 实现要点按字数占比逐条入场、当前条全亮、前一条半暗的屏显逻辑。
- 实现场景间 0.5 秒淡入与横向滑动叠化转场。
- 生成 60 秒缓拍无人声 BGM，并实现低音量衬底及首尾淡入淡出。
- 移除当前环境下无法生成 `bundle.js` 的 Rspack 强制配置，恢复默认 bundler。
- 移除视频主面板的金色边框，保留标题、角标和进度线的金色强调。
- 移除内容承载层的半透明背景、圆角和内阴影，改为纯背景构图。
- 将视频画布背景色调整为 `#070f11`。
- 建立暗色视频主题：深蓝黑背景、白色粗体标题、青色高亮要点，并让所有场景统一引用主题 token。
- 移除暗色背景中的两层圆形 `radial-gradient` 模糊效果，改为纯色背景。
- 为 7 个场景补充基于 `article.md` 的 `narration` 口播旁白。
- 使用 MiniMax 克隆音色 `myvoice_yyy` 生成 7 段场景旁白 MP3，保存到 `public/audio/`。
- 使用本地 `faster-whisper` 生成 7 段旁白逐字时间戳，并按 `storyboard.json` 原稿校正识别文本。
- 将 7 段旁白按场景起始帧接入 `Video`，并保留低音量 BGM 衬底。
- 将 BGM 峰值音量从 12% 提高到 20%，保留首尾淡入淡出。
- 将旁白字幕改为按中英文标点分隔的小句切换，并隐藏切分标点，不做逐字展开或高亮。
- 将场景时长改为以实际旁白音频为准，仅保留 0.1 秒编码尾部余量，移除额外阅读时长和 0.4 秒呼吸感造成的场景间空白。
- 将场景一信息图改为项目内透明背景的 `title.svg`、`midle.svg` 与四个区块 SVG；标题先入场，机器人随后出现，各区块再从机器人方向绘制独立引线并以无描边 SVG 沿线滑入，配合缩放回弹、轻微旋转、模糊消散和辉光动画，保留底部字幕。
- 将场景一改为“单 Agent 过载现场”：任务卡同时涌入，按旁白依次表现需求遗忘、测试跳过、代码混乱，以及页面完成但数据库和安全缺失。
- 将标题与机器人调整为约 40px 的视觉间距，保持标题先出现、机器人后出现的入场顺序。
- 将场景二改为“Agent 能力组装图”：聊天机器人、大模型、记忆、工具、目标依次接入，最后显示“理解任务并主动行动”。
- 在场景二后半段解释记忆、工具、目标时，为对应模块加入一次高亮描边、外圈脉冲和轻微弹动。
- 场景二四个模块的简介改为按具体旁白延迟入场，并为“大模型负责思考”补充高亮描边、外圈脉冲和轻微弹动。
- 将场景三改为“主管 Agent 任务看板＋团队流水线”：项目卡拆成需求、设计、开发、测试、安全，分配给角色 Agent 后汇总为可运行版本。
- 将场景四改为“单车道堵塞 vs 多车道协作”：上下文容量爆满、决策丢失后，拆成设计与后端并行推进，并由测试 Agent 独立挑错，最后显示速度与质量同时提升。
- 将场景四重构为单车道到双车道的连续变形动画，减少方框与连接线，用任务节点、容量进度、堵塞提示和测试轨道表达串行过载到并行协作。
- 将场景五改为“Agent 协作控制台”：用移动消息包表现通信，用任务派发表现分工，用冲突碰撞与重新路由表现协调。
- 将场景五重构为“协作接力”：用单一接力棒串联通信、分工、协调、冲突和重新路由，减少控制台卡片堆叠。
- 将场景六改为“同一任务切换五种协作拓扑”：依次展示主管辐射、流水线接力、圆桌协商、共享黑板和竞标调度。
- 将场景七改为风险监控台，表现通信成本上升、错误传播、权限安全责任追踪和复杂度决策；将场景八改为从混乱对话到可靠系统的三机制收束。
- 将场景七重构为“风险多米诺链”，用单条协作链、逐级放大的错误节点和治理标记集中表现风险传播。
- 将场景八重构为“混乱对话 → 三道闸门 → 可靠系统”，用气泡噪声、通信／分工／协调过滤和系统汇聚完成收束。
- 将全片状态色收敛为统一视觉风格：青绿色主色、浅蓝辅助、珊瑚红风险、薄荷绿成功、灰蓝中性，公共卡片、状态标签和数据包统一映射。
- 为全片增加统一的深海科技动态背景：深蓝渐变、低透明度移动网格、漂移光晕和轻微呼吸亮度，保持主体清晰。
- 移除场景一标题 SVG，仅保留机器人与任务过载过程，避免重复展示标题。
- 场景一六个任务小框改为从 2.5 秒起按约 0.45 秒间隔依次入场，状态提示继续按旁白时间触发。
- 场景一任务小框入场时序改为绑定对应旁白短语，需求、测试、代码、页面、数据库和安全按口播内容出现。
- 统一场景一任务框、场景二节点和通用场景卡片的 padding 与居中布局，保证标题、简介和状态在框内有稳定留白。

## 进行中

- 在浏览器中确认 Studio UI 和 `AiVideoPreview` composition 预览。

## 待办

- 在 macOS 15 或更高版本环境导出完整 MP4。

## 阻塞

- 当前会话没有可用的浏览器控制实例，无法完成可视化预览验收。
- 当前 macOS 低于 15，Remotion 4.0.523 的音频分析库缺少所需系统符号，无法在本机完成含音频 MP4 渲染。

## 最近验证

- 2026-09-11 23:08：统一主要方框内容的内边距、水平和垂直居中对齐；`npm run lint`、`npm run build` 通过，Studio 完成热构建。
- 2026-09-11 19:31：场景时长缩短为 5027 帧（167.57 秒），8 个分场时长与 `storyboard.json` 一致；`npm run lint` 与 `npm run build` 均通过。
- 2026-09-10：将场景尾部呼吸感从 1 秒缩短为 0.4 秒，保留旁白完整播放并减少结束空白。
- 2026-09-10：字幕切换改为直接使用 Whisper 逐字时间戳，修复字幕相对旁白声音逐渐错位的问题。
- 2026-09-10：精简场景 3 旁白的连续停顿结构，重新生成音频并校准 Whisper 时间戳；场景 3 音频约 21.7 秒，旁白结束后再留 1 秒呼吸感。
- 2026-09-10：按旁白实际音频时长加 1 秒呼吸感重新校准 8 个场景，避免场景 2 等旁白未播完就切页，总时长调整为 180 秒。
- 2026-09-10：为 `agent_xiezuo` 8 段旁白完成 Whisper 逐字转写，按原稿修正识别文本并输出 `public/audio/agent_xiezuo/narration-timestamps.json`。
- 2026-09-10：使用 MiniMax `myvoice_yyy` 生成 8 段 `agent_xiezuo` 旁白 MP3，并接入场景音频与 BGM 自动避让。
- 2026-09-10：进一步扩展 `agent_xiezuo` 旁白，加入案例、因果和模式解释；按每秒 5 字加 1 秒呼吸感重算为 8 场景、178 秒。
- 2026-09-10：补充 `agent_xiezuo` 8 个场景的口语化旁白，增加因果衔接与解释信息，保持屏显要点和节奏不变。
- 2026-09-10：将预览内容切换到 `content/agent_xiezuo/storyboard.json`，按屏显与旁白字数重算为 8 场景、约 106 秒，并保留逐条入场、0.5 秒淡入滑动转场和低音量循环 BGM。
- 2026-09-10：移除 `agent_xiezuo` storyboard 中的角标字段，保留标题、要点和旁白结构。
- 2026-09-10：根据 `content/agent_xiezuo/article.md` 新建 8 场景、60 秒的 `content/agent_xiezuo/storyboard.json`，完成要点长度与收尾结构校验。
- 2026-09-10：建立 `content/ai-video-demo/`，迁移 `article.md` 与 `storyboard.json` 并更新代码引用。
- 2026-09-10：画布调整为 1920×1080，旁白时长校准使总时长调整为约 61 秒；完成 15 帧转场、28px 滑动、首尾淡入淡出与 BGM 自动避让。
- 2026-09-10：字幕切分扩展到中英文常用标点，末尾标点不再显示，`npm run lint` 与 `npm run build` 均通过。
- 2026-09-10：字幕按逐字时间戳切换逗号分隔的小句，`npm run lint` 与 `npm run build` 均通过。
- 2026-09-10：字幕改为整句静态显示，移除逐字展开和高亮逻辑，`npm run lint` 与 `npm run build` 均通过。
- 2026-09-10：底部逐字字幕接入后，`npm run lint` 与 `npm run build` 均通过；代表帧渲染因当前 macOS 浏览器进程 `SIGTRAP` 未完成。
- 2026-09-10：BGM 音量调整为 20% 后，`npm run lint` 与 `npm run build` 均通过。
